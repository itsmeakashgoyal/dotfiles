# Safety net: never commit plaintext into secrets/. Only encrypted files
# (*.age), the public recipients list, and docs are allowed through. If you
# accidentally drop a plaintext secret here, git won't stage it.
*
!*/
!*.age
!.recipients
!.gitignore
!README.md
