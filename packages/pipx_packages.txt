# Python packages installed via pipx
# Install these by setting: INSTALL_PYTHON=1 before running install.sh

pyfiglet
pynvim
pylint
vim-vint
python-lsp-server[all]

# LSP plugins (installed via pipx inject, not standalone)
# These are handled separately in install.sh
# pylsp-mypy
# python-lsp-isort
# python-lsp-black
